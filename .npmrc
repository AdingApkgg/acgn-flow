# nub 供应链信任策略：按名放行 next-auth。
# next-auth v5 仍处 beta，5.0.0-beta.30 未携带 provenance 证明，而其更早发布版本带过，
# 触发 nub 默认 trustPolicy=no-downgrade 的 ERR_NUB_TRUST_DOWNGRADE 拦截。
# 该依赖是项目明确选用的（beta 线长期不带 provenance），故按名豁免；
# 仅影响 next-auth，其余包的信任检查照常。如需更严，可改为 next-auth@<具体版本>。
trustPolicyExclude=next-auth
